#include <stdio.h>

int main()
{
    int n;
    int i;
    float vect[n];
    scanf("%i", &n);
    if(n <= 50)
        for(i=0; i<=n; i++)
            scanf("%f", &vect[i]);
    for(i = 0; i<=n; i++)
        if(vect[i] % 7==0)
            c++;
            s=s+vect[i];
        


    










    return 0;
}